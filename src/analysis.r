# Date: 26 Oct 2021
# Lab05: Analysis of the relationships in Psychology data

# Name:
# Add Your Name Here


rm(list = ls()) # clear out the variables from memory to make a clean execution of the code.

# If you want to remove all previous plots and clear the console, run the following two lines.
graphics.off() # clear out all plots from previous work.

cat("\014") # clear the console

library(psych) # used for pair.panels() and also for your research of an extra approach.
library(tidyverse)

#Selecting the file
myFile  <- file.choose() # pick the csv filename
myData <- read.csv(myFile) # load the data

# Your code follows from here.






(Did you remember to add your name to this script?)
